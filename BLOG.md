# AgentProbe: 从评测到训练的完整闭环 —— AI Agent 质量工程实践

> **摘要**: 本文介绍 AgentProbe 的架构设计与真实实验——一个轻量、自托管的 AI Agent 评测平台。它解决的核心问题是:当你的 Agent 从 demo 走向生产,你如何知道它"变好还是变坏了"?本文记录了对 Qwen2.5-3B 的真实体检、一次被门禁拦截的 prompt 回归、一次判官一致性校准(Cohen's κ),以及评测→DPO 训练数据的闭环工具链。文中所有数字均来自仓库 `runs/` 与 `baselines/` 下可复现的真实运行。

---

## 1. 问题:Agent 从 POC 到生产的鸿沟

Agent 应用的普遍现状是:demo 惊艳全场,上线长期返工。核心原因有三个:

1. **链路不可观测**: Agent 的每次 LLM 调用、工具调用、重试、失败都被淹没在日志里
2. **评测靠人工肉眼看**: 每次发版前手动跑几十条 prompt,凭感觉判断"好像还行"
3. **失败没有转化为训练信号**: 分析完失败原因后写个 doc 就结束了,Agent 下次还会犯同样的错

这三个问题恰好构成一个断开的环路: **看都看不清 → 改完没信心 → 失败白失败了**。AgentProbe 把它们串成闭环:**评测 → 归因 → 造训练数据 → 微调 → 再评测**。

## 2. 设计思路:把评测做成 CI,把失败变成数据

### 2.1 评测不是"打一次分",而是"门禁 + 观测 + 归因"

| 层 | 类型 | 作用 | 参与门禁 |
|---|---|---|---|
| **Rules** | 硬断言 (contains/regex/tool_called...) | 检查"不可妥协"的约束:工具是否被正确调用?输出是否泄露了系统提示? | ✅ |
| **Trajectory** | 轨迹指标 (步数/循环/冗余/时延) | 检查"路走得好不好":有没有死循环?工具是否被冗余调用? | 部分 (no_loop, steps_within_budget) |
| **Judge** | LLM-as-Judge (1-5 分制) | 主观质量评价 | ✅ (阈值控制) |

一个用例通过,当且仅当所有门禁指标全部通过;观测指标(tokens、时延)只记录不判定。

### 2.2 可靠性 ≠ 平均正确率,引入 pass^k(并给它配置信区间)

借鉴 τ-bench 的 pass^k:同一用例重复 k 次,**k 次全过才算通过**。生产环境要的是"每次都对",不是"平均来说对"。

在 mock 演示 Agent(seed 21,可复现)上,`weather_flaky` 用例给出了一次教科书级演示:

```
pass rate 视角: 66.7%  ← "还行,修修 prompt 应该能提上去"
pass^k 视角:     0.0%  ← "根本不可用,需要系统性地解决间歇性偷懒"
```

但很快我发现一个统计陷阱:**11 个用例的样本量下,1 个用例翻转就是 9% 的波动**,单看点估计会自己骗自己。于是给两个指标都配了不确定性度量:

- `pass rate` → **Wilson 95% 置信区间**(小样本比率的标准区间估计,不依赖正态近似)
- `pass^k` → **bootstrap 95% 置信区间**(用例级重采样,seed 固定可复现)

演示运行的报告因此长这样:

```
pass rate: 78.8% (95% CI 62.3%–89.3%) · pass^k: 72.7% (95% CI 45.5%–100.0%)
```

CI 一挂出来,结论的分量立刻不同:**78.8% 的真实含义是"可能低至 62%"**,这对上线决策是质的区别。

### 2.3 判官自己也需要被评估

LLM-as-Judge 有三个已知问题:position bias(位置偏置)、self-enhancement bias、inconsistency(同一样本两次评分不一致)。

AgentProbe 做了三层缓解,前两层已实现并有真实数据:

1. **自洽性投票**: 同一评判采 n 次样(temperature>0),多数表决;
2. **pairwise 位置消偏**(已实现):A/B 对比时,同一对回答以 (A,B) 与 (B,A) 两种顺序各判一次,**两次一致才计票**,不一致计平局——位置偏置直接被协议消掉;
3. **一致性校准**(已实现工具 + 真实实验):用 Cohen's κ / 加权 κ 量化判官与人工标注(或另一个判官)的一致程度。

在没有人工标注之前,我先做了一个诚实的替代实验:**LLM 判官(qwen2.5:3b) vs 离线启发式判官,在 22 条真实运行结果上的一致性**:

```
一致率: 21/22 (95.5%) · Cohen's κ = 0.861 → almost perfect
```

κ=0.861 意味着:启发式判官可以作为 LLM 判官的**离线降级代理**(CI 里零成本、零延迟),但 1 条分歧样本也提醒我们——判官结论仍需以 rules 硬断言为主锚点。有了 `agentprobe calibrate` 命令,拿到人工标注后即可升级为"判官 vs 人类"的正式校准。

### 2.4 失败是训练数据,不是废品

归因之后,一条命令把失败案例导出为 DPO 偏好对:

```bash
agentprobe export-dpo runs/<run_id>.json --out dpo_pairs.jsonl
```

`chosen` 的构造有明确优先级:**同用例真实通过的输出 > 参考答案 expected > 放弃该失败**——不会把"运气好猜对的"当作正样本。产出的 `prompt/chosen/rejected` 直接对齐 TRL `DPOTrainer` 约定,并配套了训练脚本(见 §5)。

## 3. 真实实验:对 Qwen2.5-3B 的四次体检

被测对象:Qwen2.5-3B(本地 Ollama,OpenAI 兼容端点),内置 ReAct 工具调用循环,工具为 calculator / weather / kb_search,11 个用例。所有报告都在 `baselines/` 下。

### 3.1 第一次体检,以及"波动"的发现

| 运行 | k | 判官 | pass rate | pass^k | 判官均分 |
|---|---|---|---|---|---|
| `52412f21fd04` | 1 | LLM | 72.7% | 72.7% | 4.0 |
| `df2478888961` | 1 | LLM | 90.9% | 90.9% | 4.64 |

两次运行的配置**完全相同**,pass rate 却从 72.7% 跳到 90.9%——两个用例翻转,27% 的差别,来自 3B 模型解码的随机性本身。这个发现直接催生了三件事:k 次重复(pass^k)、置信区间、以及门禁里的显著性检验。**如果我只跑一次就下结论,这个"提升"会被当成 prompt 优化的功劳。**

### 3.2 k=3 的可靠性体检

`e5d28a25d84e`(qwen_k3_baseline.json):k=3,pass rate **75.8%**,pass^k **72.7%**。三个用例做不到"每次都过",其中包括提示注入防护的间歇性失效。

### 3.3 一次被门禁拦截的 prompt 回归(真实事故)

我写了一个"更完善"的四段式 system prompt(v2):安全约束、工具使用规范、澄清规则、效率条款,每一条都写得很细。结果:

```
# 回归对比  base=df2478888961 -> new=ae9b6207d924
| pass_rate   | 0.9091 | 0.6364 | -0.2727 |
| pass_pow_k  | 0.9091 | 0.6364 | -0.2727 |
| avg_tokens  | 603.4  | 819.5  | +216.1  |

- 回归用例: ['kb_refund', 'kb_then_math', 'prompt_injection', 'unit_convert']
- 修复用例: ['ambiguous_file']
- pass rate 下降显著性: p=0.1269 (双侧 z-test, 11 vs 11 次执行)
[FAIL] 门禁拦截: 新增失败用例 [...]; pass^k 下降 27.3%
```

两个值得展开的发现:

1. **长 prompt 适得其反**。3B 模型遵循长规则的能力有限:它开始"过度谨慎"——`kb_then_math`(查退款政策再算乘法)被它当成信息不足直接反问,工具选择反而变差(tool_selection_error ×2),注入防护条款也没救回泄露。
2. **门禁拦截的理由是样本级证据,而不是 p 值**。27.3% 的下降在 n=11 的样本量下 p=0.127,并不"统计显著";但 4 个**新失败用例**是确定性的硬信号,门禁照样拦截。这正说明:**门禁需要多层证据(新失败用例 + pass^k + 显著性),任何单层都可能漏或误杀。**

RCA 归因把这次回归解剖清楚:

```
tool_selection_error × 2 —— kb_refund / unit_convert 该用的工具没用
prompt_injection_followed × 1 —— 注入测试仍泄露系统提示
wrong_answer × 1 —— kb_then_math 关键答案未命中
```

### 3.4 这次事故的价值

如果当时没有门禁,这个 63.6% 的版本会带着"prompt 写得更专业了"的印象上线。**回归门禁不负责让 Agent 变好,它负责在你把它改坏的时候拦住你。** 这两件事同样重要。

## 4. 统计 rigor:门禁为什么不能拍脑袋

最初的实现是"pass rate 下降超 2% 就拦截"。在 11 个用例的数据集上,2% 阈值是个伪精度——1 个用例翻转就是 9%。于是门禁升级为分层证据:

1. **新失败用例**:确定性硬信号,出现即拦截;
2. **pass rate 下降超阈值且统计显著**(pooled z-test,p<0.1):拦截;
3. **下降超阈值但不显著**:只记录不拦截,防止随机波动误杀;
4. **pass^k 下降超阈值**:拦截(pass^k 本身已是严格度量)。

`compare` 同时做可比性校验:repeat 不一致、数据集不一致、用例集合不一致都会产生显式警告——**k=1 和 k=3 的 pass^k 不可直接比**,这类错误在快速迭代中极易犯。

## 5. 评测→训练闭环的现状(诚实版)

工具链已完整:

```bash
agentprobe rca runs/<run_id>.json            # 归因聚簇
agentprobe export-dpo runs/<run_id>.json     # 失败 → 偏好对
python scripts/dpo_train.py --data ...       # TRL + LoRA DPO 微调
agentprobe run && agentprobe compare         # 再评测验证
```

本次 v2 回归运行已导出 3 条真实偏好对(`examples/qwen_v2_dpo.jsonl`),演示运行导出 7 条。

**诚实声明:截至本文,DPO 微调实验尚未在真实模型上完成。** 3 条偏好对不足以支撑有效训练(DPO 通常需要数十至数百条),我不会把"样例数字"写成实验结果。这是工具链和实验设计的现状:闭环的每一环都已打通并可运行,但"微调后 pass^k 提升 X%"这句话,要等真实训练跑完才能写。这正是本项目对数字的态度:**报告里的每个数字都必须能在仓库里复现。**

## 6. 工程教训

1. **评测层数不能省**。rules 保底线(安全/格式/工具)、trajectory 保效率、judge 保质量,三层互补。在 k=3 的真实运行里,模型把系统提示以 JSON 形式吐出来但避开了字面关键词,rules 放行、judge 以 2.0 分拦下——这就是多层评测的交叉火力。
2. **pass^k + 置信区间是照妖镜**。单次运行的 90.9% 和同一配置的 72.7% 之间,只隔着一个随机种子。任何不带不确定性度量的 Agent 准确率,都是营销数字。
3. **长 prompt 不是免费的午餐**。小模型遵循长规则的能力有限,越写越细的 system prompt 可能触发过度谨慎和工具选择退化。改 prompt 必须配回归门禁。
4. **判官问题没有银弹,但可以测量**。κ=0.861 给了启发式判官离线降级的底气,也让"判官 vs 人类"的正式校准有了方法。在外部一致性被验证之前,关键结论以 rules 为准。
5. **闭环的力量来自真实性**。导出 DPO 只是起点,chosen 的构造优先级、偏好对的规模下限、训练后的再评测——每一环都决定这个飞轮是真飞轮还是 PPT 飞轮。

## 7. 后续方向

- **人工标注校准**:对 50-100 条输出做人工标注,用 `agentprobe calibrate` 得到判官 vs 人类的正式 κ;
- **跑通真实 DPO 微调**:累积偏好对到足够规模,用 `scripts/dpo_train.py` 完成一次 LoRA 微调并报告真实的 pass^k 变化;
- **在线评测**:不只跑离线数据集,也对线上真实流量做抽样评测;
- **多 Agent 场景**:multi-agent 编排下的会话级评测与归因。

---

*项目地址: [AgentProbe](https://github.com/your/agentprobe) | MIT License*
